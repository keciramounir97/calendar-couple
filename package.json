{
  "name": "calendar-couple",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "node backend/src/index.js",
    "deploy": "npm run build --prefix frontend && npx --yes firebase-tools deploy --only hosting,firestore:rules"
  }
}
